# DACON 제출 이력

2026-08-03 리더보드 초기화 이후의 전체 제출. **낮을수록 좋다** (`0.3·DINO + 0.3·Video + 0.4·Action`).

역할 분담(docs/23): 윤택 = DreamZero, 태양 = DynamiCrafter 백본 결합.

## 점수순

| 순위 | 점수 | 제출 | 담당 | 요지 |
|---:|---:|---|---|---|
| **1** | **0.2091** | `sub_s2_9000_cfg2-bg.csv` (8/15) | 윤택 | **★★★누적 9,000 · CFG 2.0 · 배경 고정** — 세 축 전부. 0.2299 대비 **−0.0208** |
| **2** | **0.2109** | `sub_aocfg2_9000-bg.csv` (8/15) | 윤택 | **순수 액션 CFG 2.0 + 배경 고정** — 표준(0.2091)과 +0.0018, 잡음 범위 |
| **3** | **0.2154** | `sub_c30_9000_cfg3-bg.csv` (8/15) | 윤택 | **CFG 3.0 + 배경 고정** — 2.0(0.2091) 대비 +0.0063, **과증폭** |
| 4 | **0.2226** | `sub_s2_9000_cfg20gr07-bgsafe.csv` (8/15) | 태양 | **CFG 2.0 + guidance_rescale 0.7** + 배경 고정 — rescale 없는 0.2091 대비 +0.0135 |
| 5 | **0.2299** | `sub_s2_9000_cfg2.csv` (8/15) | 윤택 | **★★누적 9,000 · CFG 2.0** — 0.2412 대비 **−0.0113** |
| 6 | **0.2306** | `submission_base10-4800_bg.csv` (8/14) | 윤택 | **★★배경 고정 합성** — 누적 9,000 에 후처리만. 0.2412 대비 **−0.0106** |
| 7 | **0.2358** | `submission_base10-5400_bg.csv` (8/14) | 윤택 | **배경 고정 합성** — 누적 9,600 에 후처리만. 0.2487 대비 **−0.0129** |
| 8 | **0.2412** | `submission_base10_4800.csv` (8/14) | 윤택 | **★★누적 9,000스텝** — 같은 설정, 스텝만. 7,200 대비 **−0.0460** |
| 9 | **0.2487** | `submission_base10_5400.csv` (8/14) | 윤택 | 누적 9,600 — 9,000 대비 **+0.0075**, 스텝이 뒤로 갔다 |
| 10 | **0.2630** | `sub_s3_12600_cfg1.csv` (8/15) | 윤택 | 누적 12,600 — 9,000 정점 이후 계속 나쁘다 |
| 11 | **0.2652** | `submission_b09-4200_cfg3.0.csv` (8/14) | 윤택 | **액션 CFG 3.0** — 누적 4,200. CFG 2.0(0.2695) 대비 −0.0043 |
| 12 | **0.2695** | `submission_b09-4200_cfg2.0.csv` (8/14) | 윤택 | **★액션 CFG 2.0** — 누적 4,200, 학습 동일. CFG 1.0(0.2884) 대비 **−0.0189** |
| 13 | **0.2794** | `sub_s1_10200_cfg1.csv` (8/15) | 윤택 | **누적 10,200 · CFG 1.0** — CFG 2.0판(0.2803)과 동일 → **스텝 탓** |
| 14 | **0.2872** | `submission_base10_3000.csv` (8/14) | 윤택 | **누적 7,200스텝** — 4,200 대비 −0.0012 (이 구간만 정체였다) |
| 15 | **0.2884** | `submission_base09_1800.csv` (8/14) | 윤택 | **★정적을 처음으로 깼다** — base-08 과 같은 설정, 스텝만 4,200 으로 |
| 16 | **0.3014** | `sub_s4_13200_cfg1.csv` (8/15) | 윤택 | 누적 13,200 — **정적 수준으로 되돌아갔다** |
| 17 | **0.3019** | `submission_features.csv` (8/4 19:38) | — | **정적** — 첫 프레임 16회 반복. 모델 없음 |
| 18 | **0.3030** | `submission_ft23_zeromean.csv` (8/8) | 윤택 | **잔차 + 픽셀합성 + 오프셋 제거** — 정적과 사실상 동률 |
| 19 | **0.3031** | `step0_submission.csv` (8/5 23:23) | 태양 | **1.4B DynamiCrafter 사전학습만, 파인튜닝 0스텝** |
| 20 | 0.3270 | `submission_features.csv` (8/5 21:06) | 태양 | 균등 loss 500스텝 · motion_area 0.149 |
| 21 | 0.3292 | `v1_2200_submission.csv` (8/6 19:02) | 태양 | 균등 loss ~1,750스텝 · motion_area 0.132 (U자 회복) |
| 22 | **0.3355** | `submission_ft22_composite.csv` (8/8) | 윤택 | **잔차 + 픽셀 합성** — VAE 왕복 상쇄 |
| 23 | **0.3364** | `submission_base01_300.csv` (8/9) | 윤택 | **1.4B 정상 미세조정 300스텝** — 장치 없는 순수 확산. 0스텝 0.3729 대비 −0.0365 |
| 24 | **0.3381** | `submission_base08_2100.csv` (8/14) | 윤택 | **레이아웃 수정 + 궤적요약**, 누적 2,400스텝 |
| 25 | **0.3394** | `submission_base06_2100.csv` (8/10) | 윤택 | **액션 cross-attention + 인코더 해제**, 누적 2,400스텝 |
| 26 | **0.3403** | `submission_ft20_residual.csv` (8/8) | 윤택 | **잔차 모델** — 0스텝이 정확히 정적 (ft-22 의 잠재공간 판) |
| 27 | 0.3467 | `ck1250_submission.csv` (8/6 12:36) | 태양 | 균등 loss 1,250스텝 · motion_area 0.272 |
| 28 | **0.3729** | `submission_ft01_act12_step0.csv` (8/7 22:50) | 윤택 | **1.4B 백본 정상 적재(96.8%) 0스텝 + 액션 12차원** · 코드 `09e75e5` |
| 29 | 0.3762 | `v2_500_submission.csv` (8/6 20:17) | 태양 | motion-weighted loss + 액션 lr 10배 · motion_area 0.338 |
| 30 | **0.3948** | `submission_ft05_parity.csv` (8/8) | 윤택 | ft-01 + zero-init 액션 · `fs_condition` true · `ddim_eta` 0 · `guidance_rescale` 제거 · DDIM 15 |
| 31 | 0.3957 | `step0_rff_submission.csv` (8/6 01:02) | 태양 | step0 + replace-first-frame |
| 32 | **0.3979** | `submission_base04_1500.csv` (8/10) | 윤택 | **누적 2,400스텝** — 전체 데이터셋 + 움직임가중·액션드롭아웃 |
| 33 | **0.4132** | `submission_ft07_s50.csv` (8/8) | 윤택 | ft-05 + DDIM 50 — **가장 선명한데 가장 나쁨** |
| 34 | **0.4668** | `submission_dreamzero.csv` (8/7 03:12) | 윤택 | **DreamZero-SO101 LoRA 제로샷** |
| 35 | **0.4888** | `submission_ft12_action_trained.csv` (8/8) | 윤택 | **액션 대조 손실 500스텝** — 액션은 붙었는데(gap 3σ) 점수는 악화 |
| 36 | 0.5166 | `baseline_submission.csv` (8/4 23:56) | 태양 | 대회 제공 11M UNet, scratch 학습 |

참고 — 초기화 전 기록: 정적 0.3028(7/18), IRASim FT 0.3794(7/27).

## 리더보드 (2026-08-14 23시경)

**우리 팀 = 23위 · 0.24117 · 제출 28회.** 이름 "안녕하세요홍태양임윤택입니다잘부탁드립니다".
**40위권에서 23위로 올라왔다** — 오늘 정적 정체선(0.3019)을 넘어 0.2412 까지 온 결과다.

아래는 **상위 23위까지만** 옮긴 것이다(그 아래 순위는 미수집).

| 위 | 팀 | 점수 |
|---:|---|---:|
| 1 | 김정민X김연우 | **0.13928** |
| 2 | 바이오융합시스템연구실많관부 | 0.14272 |
| 3 | 99999 | 0.15705 |
| 4 | .............. | 0.16452 |
| 5 | 인지전투 | 0.16477 |
| 6 | 내친구로봇 | 0.16802 |
| 7 | 인공지능융합 | 0.17253 |
| 8 | uuuhyun | 0.17270 |
| 9 | 스모공 | 0.17736 |
| **10** | mainunit | **0.17979** |
| 15 | CANTO.VI-학습이어긋나는 | 0.19627 |
| 19 | 공주들 | 0.22359 |
| 20 | LEXXSH | 0.22571 |
| 21 | HCI-LAB | 0.23455 |
| 22 | 동네버스 | 0.23637 |
| **23** | **우리** | **0.24117** |

**상단도 같이 내려갔다.** 8/10 에 적어둔 "1위 0.171 · 10위 0.248"과 비교하면
1위 0.171 → **0.139**, 10위 0.248 → **0.180** 이다. 우리가 오르는 동안 판 전체도 내려갔다.

**격차**: 10위까지 **0.061**, 1위까지 **0.102**. 오늘 확인된 이득이 스텝 −0.046(1회)·CFG −0.019 이니,
10위권은 그 규모의 개선이 **두세 번 더** 필요하다.

**제출 횟수 여유가 크다.** 상위권은 30~90회를 썼는데(3위 74회, 11위 91회, 15위 93회) 우리는 28회다.
로컬 채점 수단이 없는 상황에서 제출이 유일한 측정 채널이므로, 하루 10회 한도를 더 쓸 여지가 있다.

## 읽는 법 — 스무 가지가 보인다

**① 정적 정체선을 8/14 에 처음 넘었다.**
8/13 까지는 최고가 정적 0.3019, 그 아래 step0(0.3031)이 사실상 동률이었고 — 생성 모델이
만든 움직임이 **전부 순손해**였다(7월 IRASim 과 같은 구조: 방향을 못 맞추면서 크게 움직이면
가만히 있는 것보다 나쁘다). base-09(누적 4,200스텝) 0.2884 가 그 선을 −0.0135 로 넘었다.
그 뒤 스텝을 더 밀어 **누적 9,000 에서 0.2412** 까지 갔다(정적 대비 **−0.0607**). 상세는 ⑫.

**② 백본 교체는 크게 먹혔다.**
`11M scratch 0.5166 → 1.4B DynamiCrafter 사전학습 0.3031` = **−0.2135**.
대회 제공 config가 backbone.ckpt 안의 1.44B UNet을 안 쓰고 32채널 11M을 처음부터
학습하도록 돼 있었는데, 그걸 쓰기만 해도 이만큼 벌었다.

**③ ★파인튜닝이 오히려 나쁘게 만든다.**
step0(0스텝) 0.3031이 500스텝 0.3270, 1,250스텝 0.3467, 1,750스텝 0.3292보다 **전부 낫다**.
스텝을 늘려도 0스텝을 못 넘는다. 손실을 줄이는 방향과 점수를 줄이는 방향이 어긋나 있다는
뜻이고, 이건 학습 레시피(loss·lr·데이터)의 문제이지 모델 용량 문제가 아니다.
motion-weighted loss + 액션 lr 10배(0.3762)는 더 나빴다 — 움직임을 강조할수록 나빠진다.

**④ DreamZero 제로샷은 이 판에서 하위권이다.**
0.4668로 DynamiCrafter step0(0.3031)에 크게 뒤진다. 11M scratch(0.5166)보다는 낫지만
그건 비교 대상이 아니다 — **둘 다 학습 0스텝인 조건에서 DynamiCrafter가 압도한다.**

**★⑤ 백본을 더 정확히 실어도 나아지지 않았다 (2026-08-07).**
윤택이 `backbone.ckpt` 실측 shape 으로 UNet 구조 5개 항목을 정정하고
(`model_channels 320` · `channel_mult [1,2,4,4]` · `num_head_channels 64` ·
`attention_resolutions [4,2,1]` · `use_scale_shift_norm False`), `add_act_time_emb: True`
와 `only_reload_modules` 에 UNet 을 넣어 **1,468/1,517(96.8%)** 적재를 확보한 뒤 0스텝으로
제출했더니 **0.3729** 였다 — 태양님 step0(0.3031)보다 **0.0698 나쁘다**.

주의: "대회 원본 config 로는 1,107개 중 1개만 실린다"는 **원본 config** 이야기이지
태양님 config 이야기가 아니다. 태양님도 1.4B 를 올렸으니 구조 파라미터는 이미 맞췄을 것이다.
**두 실행의 정확한 차이는 태양님 config 를 못 봐서 아직 모른다.** 우리 쪽에서 다른 것으로
확실한 항목은 `use_ema: False`, 액션 **12차원**(절대6+Δ6), `add_act_time_emb: True` 셋이다.
세 가지를 한 번에 바꿔서 기여도가 분리되지 않았으므로, 다음 실험은 이걸 쪼개야 한다.

이 실험에서 별도로 확정된 것: **`use_ema: True` 는 0스텝 생성에서 치명적이다.** `LitEma` 가
사전학습 적재 이전의 랜덤 가중치를 들고 있고 `backbone.ckpt` 에 `model_ema.*` 가 없어
갱신되지 않는다 — 켜면 전 프레임이 컬러 노이즈로 나온다. 학습된 체크포인트에는 EMA 가
저장되므로 학습 후 추론에서는 드러나지 않는, **0스텝에서만 터지는 함정**이다.
자세한 내용은 `ft/README.md`.

**★⑥ 선명해질수록·안정될수록 점수가 나빠진다 (2026-08-08).**
윤택의 세 제출이 화질 사다리를 만든다. 셋 다 **학습 0스텝**이고 백본도 같다 — 바뀐 것은
샘플링 설정뿐인데, **육안 품질과 점수가 정확히 반대로 간다.**

| 제출 | 육안 | 점수 |
|---|---|---|
| ft-01 | 뒤 8프레임이 뭉개져 붕괴 | **0.3729** |
| ft-05 | 붕괴 없음, 다만 흐리고 색이 빠짐 | 0.3948 |
| ft-07 | 가장 선명·팔 색과 형태 또렷 | 0.4132 |
| (참고) 정적 | 아예 안 움직임 | **0.3019** |

읽는 법: **우리 움직임이 틀렸기 때문**이다. 틀린 움직임을 또렷하게 그릴수록 정답에서
멀어지고, 흐릿하면 어중간해서 덜 깎인다. 정적이 최고인 것도 같은 이야기의 극단이다.

**따라서 화질·안정성을 올리는 방향의 작업은 지금 전부 역효과다.** ①③⑤와 합쳐 보면,
액션 추종이 실제로 작동하기 전까지는 무엇을 개선해도 점수가 나빠진다.
남은 레버리지는 Action 항(총점의 40%) 하나뿐이다.

**★⑦ 「파인튜닝이 prior 를 망가뜨린다」는 기각됐다 — 액션 경로 자체가 손해다 (2026-08-08).**
백본 1438.86M 을 **비트 단위로 동결**하고(표본 40개가 backbone 과 완전 일치 확인) zero-init 인
`action_embed` 1.66M(0.115%)만 학습했다. 두 판을 돌렸고 차이는 손실 하나뿐이다.

| | 홀드아웃 gap | 제출 |
|---|---:|---|
| 학습 전 (zero-init) | +0.00000 | 0.3948 (= ft-05) |
| ft-10 기본 손실 600스텝 | +0.00177 (표준오차 0.00084) | 미제출 |
| ft-11 **액션 대조 손실** 500스텝 | **+0.02524 (3σ)** | **0.4888** |

두 가지가 동시에 확정된다.

1. **기본 손실로는 액션이 전혀 안 붙는다.** 600스텝을 밟아도 gap 이 잡음 수준이다. 손실이
   잠재 전체(163,840개 값)의 MSE 평균이라 배경이 기울기를 독식하고, 액션은 물어보지도 않는다.
   **액션 대조 손실**(정답 액션 vs 시간축을 섞은 액션의 예측 오차 차이)을 넣어야 붙는다.
2. **그런데 액션이 붙으니 점수가 더 나빠졌다.** 망가뜨릴 백본이 없었으므로 ③의 원인은
   "파인튜닝이 prior 를 훼손"이 아니라 **액션 경로를 통해 들어간 영향 그 자체**다.

**왜 손해인가.** 지금 구조에서 액션은 프레임당 1280차원 벡터 하나가 되어 ResBlock 의
`emb_layers`(동결)로 들어가고, 거기서 **공간 방향으로 브로드캐스트되는 채널별 상수**가 된다.
"얼마나 움직여라"는 말할 수 있어도 **"어디로"는 표현할 자리가 없다.** 그래서 학습이 만들어내는
것은 "더 움직여라"이고, 방향이 틀리면 순손해다 — ⑥과 같은 이야기다.

**따라서 다음 수는 "학습을 더/덜"이 아니라 "주입 경로를 바꾸는 것"이다.** 액션이 공간 위치를
직접 지목할 수 있는 경로가 아니면 이 천장을 못 넘는다. (태양님 v4 가 같은 진단에서 출발해
빈 텍스트 슬롯에 액션을 cross-attention 으로 넣고 있다.)

**★⑧ 미세조정이 처음으로 도움이 됐다 — 단 우리 출발점이 나빴기 때문이다 (2026-08-09).**
1.44B UNet 을 실제로 미세조정했다. 인코더(`input_blocks` 447M) 동결, 디코더+middle 994M 학습,
lr 1e-5, 유효배치 16, 300스텝(=0.5에폭). 잔차·픽셀합성 같은 장치 없는 **순수 확산 파이프라인**이다.

| | 점수 |
|---|---|
| 윤택 0스텝 (ft-01) | 0.3729 |
| **윤택 300스텝 (base-01)** | **0.3364** (−0.0365) |
| 태양 0스텝 | 0.3031 |
| 태양 500스텝 | 0.3270 (+0.0239) |

**같은 미세조정이 우리에겐 이득, 태양님에겐 손해였다.** 모순이 아니다 — 우리 0스텝이 0.0698
나빴고(⑤의 미해결 격차), 미세조정이 그중 절반을 메운 것이다. **최고 0스텝(0.3031)은 여전히
우리 300스텝(0.3364)보다 낫고, 정적(0.3019)은 아무도 못 이겼다.** ③은 뒤집히지 않았다.

**★육안: 학습이 진행되면 로봇 팔이 지워진다.** 0스텝 백본은 로봇 팔 자리에 **사람 손**을
그린다(웹 영상 사전지식). 300스텝에서 장면 붕괴는 멎고 사람 손은 가장자리에만 남는다.
그런데 600스텝에서는 **로봇 팔이 아예 사라진다.**

원인은 손실 구조다. 팔은 화면의 2~5%뿐이라 **지워도 손실이 거의 안 늘지만**, 엉뚱한 자리에
그리면 두 배로 틀린다(있어야 할 곳에 없고, 없어야 할 곳에 있음). 행선지를 모르면 지우는 게
이득이다. 즉 **팔 소멸 = 모델이 팔의 행선지를 모른다는 증거**이고, 원인 후보는
㉠ 액션을 안 쓴다 ㉡ 관절각→픽셀 대응이 데이터셋마다 카메라가 달라 원리적으로 일대다다.

태양님이 exp22 에서 IRASim 으로 본 **"정지 복사"와는 다르다** — 복사라면 팔이 그 자리에 남는다.

**★주의 — 움직임 가중 손실은 이미 한 번 실패했다.** ⑧의 팔 소멸에 대한 자연스러운 대응이
"움직이는 영역에 가중치"인데, 태양님 v2_500 이 정확히 그것이고 **0.3762 로 균등 손실
0.3270 보다 나빴다**(`motion_area` 0.149 → 0.338). 다만 그 실행은 액션 lr 10배를 같이 바꿨고
출발점도 달랐다. 우리 base-02 가 같은 아이디어를 다시 시험하는 셈이므로, **선례가 불리하다는
것을 알고 들어간다.**

**★⑨ 더 돌려도 회복하지 않았다 — 2,400스텝까지 단조 악화 (2026-08-10).**
⑧의 300스텝(0.3364) 이후 계속 밀어 **누적 2,400스텝**까지 갔다. 태양님이 가본 최대(1,750)를 넘는
구간이다. 결과는 **0.3979** — 우리 계열에서 가장 나쁘다.

| 누적 스텝 | 점수 | |
|---:|---:|---|
| 0 | 0.3729 | 윤택 0스텝 |
| 300 | **0.3364** | 여기서 한 번 좋아졌다 |
| 2,400 | 0.3979 | 계속 악화 |

**"과도기라 나쁜 것이고 더 돌리면 U자로 회복한다"는 기대는 이 레시피에서 확인되지 않았다.**
태양님 계열의 최악(1,250스텝 0.3467)보다도 나쁘다. 다만 base-04 는 base-02 에서
`motion_weight`·`action_dropout` 이 추가된 뒤의 연장이라 **'스텝'과 '손실 변경'이 섞여 있다** —
순수한 스텝 효과만 보려면 base-01 을 그대로 늘렸어야 했다.

**이 "단조 악화"는 레시피에 딸린 결론이지 스텝 자체의 성질이 아니었다** — 인코더를 풀고
`motion_weight` 를 0 으로 둔 레시피에서는 스텝이 이득으로 돌아섰다(⑫). 여기서 본 악화는
**인코더 동결 + `motion_weight` 4.0** 조건에 한정해 읽어야 한다.

**★단일 데이터셋에서도 액션을 안 따른다 (base-03).** duck 하나로 **33에폭**을 돌린 뒤
사람이 설계한 인공 액션을 넣어봤다(`base/scenario.py`) — 조건 이미지는 GT 프레임 0 으로 고정하고
`shoulder_pan` 만 왼쪽 끝까지 / 오른쪽 끝까지, 그리퍼만 여닫기, 팔만 내리기를 각각 넣었다.
**팬 오른쪽도 아래로도 팔이 같은 방향으로 갔다.** 카메라·로봇·장면이 하나뿐이라
관절각→픽셀 대응이 일대일인 조건인데도 그렇다.

⑦의 진단(주입 경로에 "어디로"를 담을 자리가 없다)과 ⑧의 진단(목적함수가 액션을 요구하지 않는다)
쪽이고, **학습량 부족은 아니다.** 33에폭이면 학습량 문제라면 풀렸어야 한다.

**★⑩ 주입 경로를 바꾸니 스텝 악화가 되돌려졌다 (2026-08-10).**
⑨에서 누적 2,400스텝이 **0.3979** 로 최악이었다. **같은 2,400스텝**에서 세 가지를 바꿔 다시 돌렸더니
**0.3394**, −0.0585 다.

| | base-04 | base-06 |
|---|---|---|
| 액션 주입 | `time_embed` 에 더하기만 | **+ cross-attention 토큰 16개/프레임** |
| 학습 범위 | 인코더 동결 (994M) | **전체 1,448.9M** (8비트 Adam) |
| `motion_weight` | 4.0 | 0 |
| **점수** | 0.3979 | **0.3394** |

**스텝이 늘수록 나빠지던 흐름이 사실상 사라졌다** — 2,400스텝이 300스텝(0.3364)과 거의 동률이다.
③("파인튜닝이 나쁘게 만든다")이 처음으로 흔들린 지점이지만, **정적(0.3019)·태양 step0(0.3031)은
여전히 못 넘었다.**

**세 변수를 한꺼번에 바꿔서 기여도가 안 갈린다.** 다음 실험은 쪼개야 한다 —
cross-attention 만 / 인코더 해제만 / `motion_weight` 만.

**구현 메모.** 킷 UNet 은 `context` 길이가 `77 + t*16` 이면 이미지 토큰을 프레임별로 펼치는
분기를 이미 갖고 있다(`openaimodel3d.py:719`). 그 슬롯에 액션 토큰을 **더하면** 킷 코드를
한 줄도 안 고치고 프레임별 cross-attention 이 되고, 무조건부 분기(`uc`, 길이 93)에는 액션이
안 들어가므로 액션 CFG 도 그대로 성립한다. zero-init 이라 시작 시점 출력이 원본과
`torch.equal` 로 일치함을 확인했다. 자세한 건 `base/README.md`.

**★⑪ 레이아웃 버그를 고쳐도 점수가 안 변했다 — cross-attention 은 기여자가 아니었다 (2026-08-14).**
⑩의 base-06 은 **context 레이아웃이 망가진 채** 학습됐다. Resampler 가 내는 이미지 토큰이
16개가 아니라 `num_queries × video_length = 256`개인데(`resampler.py:118`), 뒤 16개만 이미지로
보고 앞 317개를 텍스트로 잘라 context 길이가 333 → **573** 이 됐다. base-08 은 이걸 고치고
(텍스트 77 + 프레임별 이미지 256) **궤적요약 토큰 32개**까지 빈 텍스트 슬롯에 더했다.

| 누적 2,400스텝 | 액션 주입 | 레이아웃 | 학습 범위 | `motion_weight` | 점수 |
|---|---|---|---|---|---|
| base-04 | 덧셈만 | — | 인코더 동결 | 4.0 | 0.3979 |
| base-06 | + cross-attn | **573 (버그)** | 전체 | 0 | 0.3394 |
| **base-08** | + cross-attn + 궤적요약 | **333 (정상)** | 전체 | 0 | **0.3381** |

**−0.0013.** 레이아웃을 고치고 토큰을 더 넣어도 사실상 그대로다. 그러면 ⑩에서 본
0.3979 → 0.3394(−0.0585)는 **cross-attention 이 제대로 붙어서가 아니다** — 남은 두 변수,
즉 **인코더 동결 해제**와 **`motion_weight` 4.0→0** 중 하나(또는 둘)가 한 일이다.

cross-attention 경로는 **망가진 채로도 고친 뒤에도 같은 점수**를 낸다. 액션을 실을 자리를
바꾼 것만으로는 아무 일도 일어나지 않았다는 뜻이고, ⑦의 진단("주입 경로를 바꿔야 한다")은
**경로를 바꾸는 것만으로는 부족하다**로 좁혀진다. 목적함수가 액션을 쓰라고 요구하지 않으면
새 경로도 그냥 안 쓰인다.

**다음 실험은 남은 두 변수를 쪼개는 것이다** — `motion_weight` 만 되돌린 판, 인코더만 다시 얼린 판.

**★★⑫ 정적 정체선을 넘었다 — 스텝을 더 넣은 것뿐이다 (2026-08-14).**
base-08(누적 2,400, 0.3381)에서 **설정을 하나도 안 바꾸고** 1,800스텝만 더 돌린 base-09 가
**0.2884** 다. 정적 0.3019 대비 **−0.0135**, base-08 대비 **−0.0497**.

| 누적 스텝 | 점수 | 학습 범위 · `motion_weight` |
|---:|---:|---|
| 300 (base-01) | 0.3364 | 인코더 동결 · 4.0 |
| 2,400 (base-04) | 0.3979 | 인코더 동결 · 4.0 |
| 2,400 (base-08) | 0.3381 | 전체 · 0 |
| **4,200 (base-09)** | **0.2884** | 전체 · 0 |
| **7,200 (base-10)** | **0.2872** | 전체 · 0 |

**⑨의 "스텝을 늘리면 단조 악화"와 ③의 "파인튜닝이 오히려 나쁘게 만든다"가 여기서 깨진다.**
같은 레시피 안에서 스텝만 늘렸는데 −0.0497 이 나왔다. 두 관측 모두 **인코더 동결 +
`motion_weight` 4.0** 조건에서 얻은 것이었고, 그 조건을 풀자 스텝의 부호가 뒤집혔다.

이 결과는 ⑪을 다시 읽게 한다. base-08 이 base-06 과 동률(−0.0013)이었던 것을
"cross-attention 은 기여자가 아니다"로 읽었는데, **2,400스텝이 아직 수렴 전 구간이라
그 지점에서는 어떤 변경도 점수에 안 드러났을** 가능성이 생긴다. 2,400 에서 갈리지 않은
변수들을 4,200 이후에서 다시 재봐야 판단이 선다.

**★★스텝 축은 살아 있다 — 중간의 평평한 구간에 속았다 (같은 날 밤).**

| 누적 | 점수 | 구간 | 스텝당 |
|---:|---:|---:|---:|
| 2,400 | 0.3381 | | |
| 4,200 | 0.2884 | −0.0497 / 1,800 | −2.8e-5 |
| 7,200 | 0.2872 | −0.0012 / 3,000 | −4.0e-7 |
| **9,000** | **0.2412** | **−0.0460 / 1,800** | **−2.6e-5** |

7,200 을 보고 "기울기가 70배 꺾였다"고 읽었는데 **9,000 에서 2,400→4,200 과 같은 기울기로
돌아왔다.** 4,200~7,200 은 기울기 소멸이 아니라 **그 구간만의 정체**였다.

**이 판단 실수를 하루에 두 번 했다.** 4,200 을 보고 "스텝 축이 살아났다"(⑫ 초판), 7,200 을 보고
"꺾였다", 9,000 에서 다시 뒤집혔다. **중간 한두 점으로 추세를 판정하지 말 것** — 이 곡선은
계단식이고, 평평한 구간이 3,000스텝 이어져도 그 뒤에 다시 떨어진다.

**남은 예산.** 재현 체인(backbone → 300 → 2,100 → 1,800 → 4,800 = 9,000스텝)은 PRO 6000
실측 10.3초/스텝으로 **25.8시간**, 규칙 5 의 96시간 중 27% 다. 스텝이 아직 값을 하므로
**예산을 계속 쓰는 것이 지금 가장 확실한 수다.**

**인프라 메모.** V100 SSH 포트가 30012 → **30019** 로 옮겨져 있었다. 같은 NodePort 대역에
**남의 파드 sshd 가 10개** 떠 있어 포트 스캔만으로는 못 고르고, 키 인증이 통과하는 것으로
찾아야 한다(호스트명 `jupyter-1169--1042`). 5090→V100 직결 전송이 100MB/s 라 5.5GB
체크포인트 이동이 56초다 — **학습을 멈추지 않고 V100 에서 병렬 생성**할 수 있다.

**★★⑬ 액션 CFG 가 총점에 이롭다 — 학습 없이 얻는 이득 (2026-08-14 밤).**
누적 4,200 체크포인트에서 **학습은 그대로 두고 생성의 액션 CFG 만 1.0 → 2.0** 으로 올렸다.
한 변수만 바뀐 대조다.

| 누적 4,200 | 액션 CFG | 점수 |
|---|---:|---:|
| base-09 1800 | 1.0 | 0.2884 |
| **같은 체크포인트** | **2.0** | **0.2695** (−0.0189) |

**학습 700~1,300스텝어치의 이득을 생성 설정 한 줄로 얻는다.** ⑦에서 "액션 경로가 손해"라고
읽었고 어텐션 측정에서도 액션이 출력의 1% 미만이었는데, **그 작은 신호의 방향은 옳았다** —
증폭하니 이득이 나온다. 약한 것과 틀린 것은 다르다.

**단 공짜는 아니다.** 윤택 육안으로 **Video 성분에서 감점되는 것으로 보인다** — 총점이
좋아진 것은 Action 이득이 Video 손실보다 컸기 때문이다. 성분별 값은 로컬에서 못 재므로
(킷 채점기는 2026-08-03 공지로 사용 금지) 확증은 없다. CFG 를 더 올릴 때 이 손실이 먼저
한계에 닿을 수 있다.

**추론 시간은 1.8배다** (조건부·무조건부 두 번 통과). Runpod RTX 5090 실측 15.0초/샘플,
PRO 6000 환산 **약 13.1초** — 한도 16.7초 안에 들어오지만 여유가 3.6초뿐이다.
DDIM 스텝을 건드리면 이 여유가 바뀌므로 같이 계산해야 한다.

**★⑭ 두 축 모두 수확체감에 들어갔다 — 그리고 Public 잡음이 판정을 가린다 (2026-08-14 밤).**

**스텝 축** (설정 동일, 스텝만):

| 누적 | 점수 | |
|---:|---:|---|
| 7,200 | 0.2872 | |
| **9,000** | **0.2412** | −0.0460 |
| 9,600 | 0.2487 | **+0.0075 (후퇴)** |

**CFG 축** (누적 4,200 고정, 생성 설정만):

| CFG | 점수 | |
|---:|---:|---|
| 1.0 | 0.2884 | |
| 2.0 | 0.2695 | −0.0189 |
| 3.0 | 0.2652 | −0.0043 (이득 4.4배 감소) |

**★그런데 Public 은 약 65샘플이라 잡음이 크다**([03](03_데이터분석.md)). 9,600 의 +0.0075 도,
CFG 3.0 의 −0.0043 도 **잡음으로 설명될 수 있는 크기**다. 따라서 읽을 수 있는 것은
"9,600 에서 개선이 확인되지 않았다"와 "CFG 2.0 과 3.0 은 사실상 동급"까지이지,
"9,600 이 나쁘다" 또는 "3.0 이 낫다"가 아니다.

**확실한 것만 남기면**: 최고는 **누적 9,000 · CFG 1.0 = 0.2412**, CFG 는 1.0 → 2.0 에서
**−0.0189** 가 확실하다. 이 둘을 합친 판(누적 10,200 · CFG 2.0)이 다음 확인 대상이다.

**★★⑮ 배경 고정 합성 — 학습도 생성도 안 건드리고 −0.012 (2026-08-14 밤).**
생성된 16프레임에서 움직이지 않아야 할 픽셀을 조건 프레임으로 되돌린다([bgfreeze.py](../base/bgfreeze.py)).
**같은 개입을 두 기준선에 얹어** 확인했다.

| 누적 | 원본 | + 배경 고정 | |
|---:|---:|---:|---:|
| 9,000 | 0.2412 | **0.2306** | −0.0106 |
| 9,600 | 0.2487 | **0.2358** | −0.0129 |

**두 판 모두 같은 방향·비슷한 폭이라 잡음이 아니다.** 그리고 **비용이 0 이다** — GPU 를 안 쓰고
216개에 90초, 이미 만들어둔 mp4 에 사후로 얹는다. 어떤 체크포인트·어떤 CFG 와도 조합된다.

**`step0_rff` 의 +0.093 실패와 무엇이 다른가.** 그건 **한 프레임만** 원본으로 갈아끼워
교체 프레임과 생성 프레임 사이에 시간적 불연속이 생겼다. 여기서는 ① 16프레임 **전부**에
② **같은 마스크**(프레임별로 새로 만들지 않는다)를 ③ **페더링**과 함께 적용해 불연속이 없다.

**설정**: 프레임 0 대비 변화량의 시간축 최대에서 상위 10%(q=0.90)를 동적으로 보고,
6픽셀 팽창 + 8픽셀 페더링, 배경에도 생성본 15% 잔류 → 화면의 약 34% 가 생성본 유지.
팔을 지우면 Action 이 무너지므로 **넉넉히 살리는 쪽으로 편향**시킨 값이다. 더 공격적인 설정
(floor 0, q 0.95)은 아직 안 재봤다.

**★⑯ 스텝과 CFG 는 더해지지 않는다 — 누적 9,000 이 정점으로 보인다 (2026-08-15).**
각각 따로는 이득이었던 둘을 합쳤더니 **양쪽 기준선보다 나빠졌다.**

| 판 | 점수 | |
|---|---:|---|
| 누적 9,000 · CFG 1.0 | **0.2412** | 최고 |
| 누적 4,200 · CFG 2.0 | 0.2695 | |
| **누적 10,200 · CFG 2.0** | **0.2803** | 9,000 대비 **+0.0391** |

CFG 1.0 계열의 스텝 축도 이미 후퇴 중이었다(9,000 0.2412 → 9,600 0.2487).
**두 축 모두 9,000 부근에서 한계에 닿았다고 읽는 것이 지금 관측과 가장 잘 맞는다.**

**가설(미검증).** 모델이 액션을 잘 쓰게 될수록 CFG 증폭이 과해진다 — 액션 신호가 약한
4,200 에서만 증폭이 남는 이득이었을 수 있다. **가르려면 누적 9,000 에 CFG 2.0 한 판**이
필요하다(같은 체크포인트에서 CFG 만 바뀌는 대조). 지금까지 CFG 를 잰 것은 전부 4,200 뿐이다.

**교훈.** 이 판에서 두 변수를 한 번에 바꿨더니 또 기여도가 안 갈렸다 — ⑩에서 같은 실수를
하고 ⑪로 되짚었는데 반복했다. **한 판에 한 변수.**

**★★⑰ 스텝은 9,000 에서 끝났고, CFG 는 계속 이롭다 — ⑯의 가설은 틀렸다 (2026-08-15).**

**스텝 축** (전부 CFG 1.0, 설정 동일):

| 누적 | 점수 |
|---:|---:|
| 4,200 | 0.2884 |
| 7,200 | 0.2872 |
| **9,000** | **0.2412** ← 정점 |
| 9,600 | 0.2487 |
| 10,200 | 0.2794 |
| 12,600 | 0.2630 |
| 13,200 | **0.3014** (정적 수준) |

**9,000 이후 네 점이 전부 나쁘고 13,200 에서 정적(0.3019)까지 되돌아갔다.**
곡선이 단조롭지 않게 오르내리지만(10,200 → 12,600 은 오히려 개선) 정점은 9,000 이 분명하다.
**스텝 축은 닫혔다 — 5090 의 장기 학습을 계속할 근거가 없다.**

**CFG 축** (체크포인트 고정, 생성 설정만):

| 체크포인트 | CFG 1.0 | CFG 2.0 | 차이 |
|---|---:|---:|---:|
| 누적 4,200 | 0.2884 | 0.2695 | −0.0189 |
| **누적 9,000** | 0.2412 | **0.2299** | **−0.0113** |
| 누적 10,200 | 0.2794 | 0.2803 | +0.0009 |

**⑯에서 "액션을 잘 쓰는 체크포인트일수록 CFG 증폭이 과해진다"고 가설을 세웠는데 틀렸다.**
9,000 은 우리 최고 체크포인트인데 CFG 가 −0.0113 을 준다. 10,200 에서 CFG 가 무효였던 것은
**그 체크포인트 자체가 나빴기 때문**이지 CFG 의 성질이 아니다(CFG 1.0 0.2794 vs 2.0 0.2803).

**세 축의 현재 정리**:
- **스텝** — 9,000 에서 종료. 더 밀 이유 없음
- **CFG** — 좋은 체크포인트에서 −0.011, 값 2.0~3.0 대. 유효
- **배경 고정** — −0.012, 후처리라 공짜. 유효

**아직 안 낸 최선 조합**: 누적 9,000 · CFG 2.0 · **배경 고정** (예상 0.218 부근).

**★★⑱ 배경 고정과 CFG 는 서로를 보완한다 (2026-08-15).**

| 누적 9,000 | 원본 | + 배경 고정 | 이득 |
|---|---:|---:|---:|
| CFG 1.0 | 0.2412 | 0.2306 | −0.0106 |
| **CFG 2.0** | 0.2299 | **0.2091** | **−0.0208** |

**배경 고정의 이득이 CFG 판에서 두 배**다. CFG 는 액션을 증폭하면서 배경 잡음도 같이 키우고,
배경 고정이 정확히 그 손실을 걷어낸다. 윤택이 관측한 "CFG 는 Video 성분에서 감점된다"와
정합적이며, **그 감점을 후처리로 되돌릴 수 있다**는 뜻이다.

따라서 두 축을 따로 재서 더하면 과소평가된다 — CFG 를 더 올렸을 때의 상한도 다시 봐야 한다
(CFG 3.0 은 4,200 에서만 재봤고, 배경 고정과 함께는 아직 안 재봤다).

**정적(0.3019) 대비 −0.0928.** 8/14 아침 0.3381 에서 하루 반 만에 −0.129 다.

**★⑲ CFG 는 액션 + 이미지를 같이 증폭하고 있었다 — 그런데 실효는 없었다 (2026-08-15).**

태양님·GPT 가 코드에서 짚은 지적은 **사실로 확정**됐다. 킷의 `prepare_batch_for_inference`
(`ddpm3d.py`)가 만드는 무조건부 분기 `uc` 는 **검은 이미지의 CLIP 토큰**을 쓴다:

```python
img = torch.zeros_like(x[:, :, 0])          # 검은 이미지
uc = torch.cat([uc_prompt, self.image_proj_model(self.embedder(img))], dim=1)
```

우리는 `get_batch_input` 만 덮어썼고 이 함수는 안 건드렸다. 그래서 CFG 2.0 은 액션뿐 아니라
**"실제 첫 프레임 vs 검은 화면"** 차이까지 2배로 증폭하고 있었다.

**책임 소재**: 이 `uc` 구성은 **DynamiCrafter 원본 설계**다(업스트림 `log_images` 에 동일 코드 확인).
주최측은 그 패턴을 대회 추론 함수로 옮겨 담았을 뿐이고, **베이스라인은 CFG 를 끈 채(scale 1.0,
`action_dropout_prob` 0.0) 출하**해 그 경로가 실행조차 안 된다. **우리가 두 스위치를 켜면서
`uc` 가 무엇인지 확인하지 않은 것**이 원인이다.

**그런데 고쳐도 점수가 안 변했다**: `uc` 를 "같은 이미지 + 액션만 제거"로 바꿔 다시 돌렸더니
**0.2091 → 0.2109 (+0.0018, 잡음 범위)**.

| 누적 9,000 · CFG 2.0 · 배경 고정 | 점수 |
|---|---:|
| 표준 uc (검은 이미지) | **0.2091** |
| 순수 액션 uc | 0.2109 |

**배경 고정이 이미 이미지 증폭의 손실을 걷어내고 있었기 때문**으로 보인다. ⑱에서 "배경 고정이
CFG 의 부작용을 상쇄한다"고 읽었는데, 그 부작용의 정체가 바로 이 이미지 증폭이었다.

**결론**: 지적은 옳았고 이해는 교정됐지만 **처방은 이미 갖고 있었다**. 표준 CFG 를 계속 써도 된다.
구현은 `base/generate.py --action-only-cfg` 로 남겨둔다.

**아직 안 고친 것 (P0-2)**: 액션 드롭아웃이 **두 경로에서 따로 추첨**된다 — 우리 `model.py:244`
(cross-attn 토큰)와 킷 `openaimodel3d.py:739`(덧셈 경로)가 독립적으로 동전을 던진다. p=0.1 이면
**둘 다 떨어지는 경우가 1%** 뿐이라(의도 10%) 추론의 uncond 상태가 덜 학습됐다. 재학습이 필요해
아직 미검증이다.

**★⑳ CFG 값 축도 닫혔다 — 최적은 2.0, 체크포인트가 좋을수록 낮아진다 (2026-08-15).**

| 누적 9,000 · 배경 고정 | CFG | 점수 |
|---|---:|---:|
| | 1.0 | 0.2306 |
| | **2.0** | **0.2091** |
| | 3.0 | 0.2154 |

1.0 → 2.0 에서 −0.0215 를 벌고 3.0 에서 +0.0063 되돌아간다. **2.0 부근이 정점**이다.

**★체크포인트가 좋아질수록 최적 CFG 가 낮아진다.** 누적 4,200(배경 고정 없음)에서는
CFG 3.0(0.2652)이 2.0(0.2695)보다 나았는데 누적 9,000 에서는 뒤집혔다. 액션 신호가 약한
모델일수록 증폭이 더 필요하고, 잘 학습된 모델은 2.0 이면 충분하다.

**세 축이 모두 정점에 닿았다**: 스텝 9,000(⑰) · CFG 2.0(⑳) · 배경 고정(⑮, 파라미터 미탐색).
현재 최고 **0.2091** 은 이 세 값의 조합이고, **같은 축을 더 밀어서 얻을 것은 없다.**
남은 것은 학습 레시피(`motion_weight`, 드롭아웃 공유, 손실 재배분) 쪽이다.

## 판정

**DynamiCrafter 경로가 앞선다.** 같은 무학습 조건에서 0.3031 vs 0.4668이고, 모델도
1.4B vs 14B라 학습·추론 비용이 10분의 1이다.

**"더 학습하면 된다"가 8/14 에 처음 근거를 얻었다** (⑫). 8/13 까지는 ③·⑨가 정반대를
가리켰지만, 그 관측들은 **인코더 동결 + `motion_weight` 4.0** 조건에 묶여 있었다.
그 조건을 푼 레시피에서는 2,400 → 4,200스텝이 −0.0497 이다. 아래 후보들은 **그 조건에서
왜 학습이 점수를 깎았는지**에 대한 설명으로 읽어야 한다:

- 우리가 최적화하는 ε-MSE(latent 전체 평균)에 로봇 팔 정확도 신호가 사실상 없다.
  팔은 화면의 몇 %라 배경 복원이 gradient를 다 가져간다.
- 학습이 진행되며 모델이 "더 크게 움직이는" 쪽으로 가는데, 방향이 틀리면 그게 순손해다
  (`motion_area` 0.132 → 0.338 구간에서 점수가 나빠지는 것과 일치).
- 평가 지표가 정지에 유리하다. 움직임의 기대 이득이 음수라면 학습은 손해를 키운다.

`motion_area`가 점수와 U자 관계를 보인다는 태양님 관측이 이 방향을 뒷받침한다 —
**적정 움직임량이 존재하고 그걸 넘으면 손해**라는 뜻이다.

## 다음에 필요한 것

- **품질을 잴 로컬 수단.** 킷 채점기는 2026-08-03 공지로 못 쓴다. 제출(하루 10회)이
  유일한 측정 채널이라 실험 회전이 느리다. 자체 IDM 학습이 여기서 값을 한다.
- **움직임량 통제.** `motion_area` 같은 대리 지표로 생성물의 움직임을 조절하는 쪽이
  손실을 더 줄이는 것보다 직접적일 수 있다.
