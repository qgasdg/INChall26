# DACON 제출 이력

2026-08-03 리더보드 초기화 이후의 전체 제출. **낮을수록 좋다** (`0.3·DINO + 0.3·Video + 0.4·Action`).

역할 분담(docs/23): 윤택 = DreamZero, 태양 = DynamiCrafter 백본 결합.

## 점수순

| 순위 | 점수 | 제출 | 담당 | 요지 |
|---:|---:|---|---|---|
| 1 | **0.3019** | `submission_features.csv` (8/4 19:38) | — | **정적** — 첫 프레임 16회 반복. 모델 없음 |
| 2 | **0.3031** | `step0_submission.csv` (8/5 23:23) | 태양 | **1.4B DynamiCrafter 사전학습만, 파인튜닝 0스텝** |
| 3 | 0.3270 | `submission_features.csv` (8/5 21:06) | 태양 | 균등 loss 500스텝 · motion_area 0.149 |
| 4 | 0.3292 | `v1_2200_submission.csv` (8/6 19:02) | 태양 | 균등 loss ~1,750스텝 · motion_area 0.132 (U자 회복) |
| 5 | 0.3467 | `ck1250_submission.csv` (8/6 12:36) | 태양 | 균등 loss 1,250스텝 · motion_area 0.272 |
| 6 | 0.3762 | `v2_500_submission.csv` (8/6 20:17) | 태양 | motion-weighted loss + 액션 lr 10배 · motion_area 0.338 |
| 7 | 0.3957 | `step0_rff_submission.csv` (8/6 01:02) | 태양 | step0 + replace-first-frame |
| 8 | **0.4668** | `submission_dreamzero.csv` (8/7 03:12) | 윤택 | **DreamZero-SO101 LoRA 제로샷** |
| 9 | 0.5166 | `baseline_submission.csv` (8/4 23:56) | 태양 | 대회 제공 11M UNet, scratch 학습 |

참고 — 초기화 전 기록: 정적 0.3028(7/18), IRASim FT 0.3794(7/27).
현 리더보드: 1위 0.171 · 10위 0.248.

## 읽는 법 — 네 가지가 보인다

**① 아직 아무것도 정적을 못 이겼다.**
최고가 정적 0.3019이고, 2위 step0(0.3031)이 사실상 동률이다. **생성 모델이 만든 움직임이
지금까지 전부 순손해**다. 7월에 IRASim에서 배운 것과 같은 구조 — 방향을 못 맞추면서
크게 움직이면 가만히 있는 것보다 나쁘다.

**② 백본 교체는 크게 먹혔다.**
`11M scratch 0.5166 → 1.4B DynamiCrafter 사전학습 0.3031` = **−0.2135**.
대회 제공 config가 backbone.ckpt 안의 1.44B UNet을 안 쓰고 32채널 11M을 처음부터
학습하도록 돼 있었는데, 그걸 쓰기만 해도 이만큼 벌었다.

**③ ★파인튜닝이 오히려 나쁘게 만든다.**
step0(0스텝) 0.3031이 500스텝 0.3270, 1,250스텝 0.3467, 1,750스텝 0.3292보다 **전부 낫다**.
스텝을 늘려도 0스텝을 못 넘는다. 손실을 줄이는 방향과 점수를 줄이는 방향이 어긋나 있다는
뜻이고, 이건 학습 레시피(loss·lr·데이터)의 문제이지 모델 용량 문제가 아니다.
motion-weighted loss + 액션 lr 10배(0.3762)는 더 나빴다 — 움직임을 강조할수록 나빠진다.

**④ DreamZero 제로샷은 이 판에서 하위권이다.**
0.4668로 DynamiCrafter step0(0.3031)에 크게 뒤진다. 11M scratch(0.5166)보다는 낫지만
그건 비교 대상이 아니다 — **둘 다 학습 0스텝인 조건에서 DynamiCrafter가 압도한다.**

## 판정

**DynamiCrafter 경로가 앞선다.** 같은 무학습 조건에서 0.3031 vs 0.4668이고, 모델도
1.4B vs 14B라 학습·추론 비용이 10분의 1이다.

**다만 "더 학습하면 된다"는 근거가 없다.** ③이 정반대를 가리킨다. 지금 필요한 것은
학습량이 아니라 **왜 학습이 점수를 깎는지**를 밝히는 것이다. 후보:

- 우리가 최적화하는 ε-MSE(latent 전체 평균)에 로봇 팔 정확도 신호가 사실상 없다.
  팔은 화면의 몇 %라 배경 복원이 gradient를 다 가져간다.
- 학습이 진행되며 모델이 "더 크게 움직이는" 쪽으로 가는데, 방향이 틀리면 그게 순손해다
  (`motion_area` 0.132 → 0.338 구간에서 점수가 나빠지는 것과 일치).
- 평가 지표가 정지에 유리하다. 움직임의 기대 이득이 음수라면 학습은 손해를 키운다.

`motion_area`가 점수와 U자 관계를 보인다는 태양님 관측이 이 방향을 뒷받침한다 —
**적정 움직임량이 존재하고 그걸 넘으면 손해**라는 뜻이다.

## 다음에 필요한 것

- **품질을 잴 로컬 수단.** 킷 채점기는 2026-08-03 공지로 못 쓴다. 제출(하루 10회)이
  유일한 측정 채널이라 실험 회전이 느리다. 자체 IDM 학습이 여기서 값을 한다.
- **움직임량 통제.** `motion_area` 같은 대리 지표로 생성물의 움직임을 조절하는 쪽이
  손실을 더 줄이는 것보다 직접적일 수 있다.
